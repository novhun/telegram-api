Run the application:

bash
npm start
Build for production:

bash
npm run build
Access URLs:
Development: http://localhost:3000/web

Production build: Deploy the build folder to your server

API Base: http://127.0.0.1:9000

The application will now work with:

Base path: /web

All API calls to: http://127.0.0.1:9000

All navigation within the /web path

Proper export functionality for all data

Complete API documentation

Modern, responsive UI with dark theme